# Legacy Training Configs

This folder contains the older `train_*.toml` RLM RLVR training configs that predate the latest 9B-derived ablation plan in `../ABLATION_PLAN.md`.

Current active work should stay in `configs/rlm_rlvr/` and be based on the newer 9B `r2_turn20` family.
